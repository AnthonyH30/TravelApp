import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    image: {
        width: '100%',
        height: 400,
        borderRadius: 20,
        marginBottom: 24,
        paddingTop: 24
    },
    backContainer: {
        position: 'absolute',
        margin: 32,
    },
    backIcon: {
        height: 40,
        width: 40
    }
})

export default styles;