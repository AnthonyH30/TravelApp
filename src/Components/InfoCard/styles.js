import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    icon: {
        height: 40,
        width: 40
    },
    title: {
        fontSize: 12,
        color: '#000',
    }
})

export default styles;