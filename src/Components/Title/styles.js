import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 32,
        // color: '#4681a3',
        color: '#ff0064',
        fontWeight: "bold",
    }
})

export default styles;